docs/
.venv/
pnpm-lock.yaml

# 生成产物：格式由生成器决定，prettier 介入只会和它打架
packages/contracts/generated/
# Next 每次 build 都会重写它，本地格式化过、CI 新生成的没有——正是这种
# 「本地绿 CI 红」的来源
apps/web/next-env.d.ts
**/dist/
**/.next/
**/.turbo/

# Markdown 手工排版：中英混排的表格列宽是人对齐的，prettier 会打乱且不带来可读性收益
*.md
