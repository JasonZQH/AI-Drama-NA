{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src/**/*.ts"],
  // 测试不进 dist：否则 vitest 会把 src 与编译产物各跑一遍，用例数悄悄翻倍
  "exclude": ["src/**/*.test.ts"]
}
